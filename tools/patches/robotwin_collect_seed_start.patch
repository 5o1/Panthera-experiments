diff --git a/script/collect_data.py b/script/collect_data.py
--- a/script/collect_data.py
+++ b/script/collect_data.py
@@ -103,7 +103,11 @@ def main(task_name=None, task_config=None):
 
 
 def run(TASK_ENV, args):
-    epid, suc_num, fail_num, seed_list = 0, 0, 0, []
+    seed_start = int(args.get("seed_start", 0))
+    if seed_start < 0:
+        raise ValueError("seed_start must be nonnegative")
+    retry_delay_s = float(args.get("retry_delay_s", 1.0))
+    epid, suc_num, fail_num, seed_list = seed_start, 0, 0, []
 
     print(f"Task Name: \033[34m{args['task_name']}\033[0m")
 
@@ -119,7 +123,7 @@ def run(TASK_ENV, args):
                 if len(seed_list) != 0:
                     seed_list = [int(i) for i in seed_list]
                     suc_num = len(seed_list)
-                    epid = max(seed_list) + 1
+                    epid = max(max(seed_list) + 1, seed_start)
             print(f"Exist seed file, Start from: {epid} / {suc_num}")
 
         while suc_num < args["episode_num"]:
@@ -150,7 +154,8 @@ def run(TASK_ENV, args):
 
                 if args["render_freq"]:
                     TASK_ENV.viewer.close()
-                time.sleep(0.3)
+                if retry_delay_s > 0:
+                    time.sleep(retry_delay_s)
             except Exception as e:
                 # stack_trace = traceback.format_exc()
                 print(" -------------")
@@ -164,6 +169,7 @@ def run(TASK_ENV, args):
 
                 if args["render_freq"]:
                     TASK_ENV.viewer.close()
-                time.sleep(1)
+                if retry_delay_s > 0:
+                    time.sleep(retry_delay_s)
 
             epid += 1
