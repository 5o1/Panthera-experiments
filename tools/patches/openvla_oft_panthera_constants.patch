diff --git a/prismatic/vla/constants.py b/prismatic/vla/constants.py
--- a/prismatic/vla/constants.py
+++ b/prismatic/vla/constants.py
@@ -46,5 +46,12 @@ BRIDGE_CONSTANTS = {
 }
 
+PANTHERA_CONSTANTS = {
+    "NUM_ACTIONS_CHUNK": 25,
+    "ACTION_DIM": 7,
+    "PROPRIO_DIM": 7,
+    "ACTION_PROPRIO_NORMALIZATION_TYPE": NormalizationType.BOUNDS_Q99,
+}
+
 
 # Function to detect robot platform from command line arguments
 def detect_robot_platform():
@@ -55,6 +62,7 @@ def detect_robot_platform():
             'LIBERO': 'LIBERO',
             'ALOHA': 'ALOHA',
             'BRIDGE': 'BRIDGE',
+            'PANTHERA': 'PANTHERA',
         }
         if robot_env in env_mapping:
             print(f"Detected robot platform from environment: {env_mapping[robot_env]}")
@@ -83,6 +91,8 @@ elif ROBOT_PLATFORM == "ALOHA":
     constants = ALOHA_CONSTANTS
 elif ROBOT_PLATFORM == "BRIDGE":
     constants = BRIDGE_CONSTANTS
+elif ROBOT_PLATFORM == "PANTHERA":
+    constants = PANTHERA_CONSTANTS
 
 # Assign constants to global variables
 NUM_ACTIONS_CHUNK = constants["NUM_ACTIONS_CHUNK"]
