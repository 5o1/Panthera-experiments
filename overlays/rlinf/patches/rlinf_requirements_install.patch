diff --git a/requirements/install.sh b/requirements/install.sh
--- a/requirements/install.sh
+++ b/requirements/install.sh
@@ -2686,11 +2686,13 @@ install_robotwin_env() {
     }
     cuda_major="${cuda_mm%% *}"
     cuda_minor="${cuda_mm##* }"
-    if [ "$cuda_major" -gt 12 ] || { [ "$cuda_major" -eq 12 ] && [ "$cuda_minor" -ge 8 ]; }; then
+    if [ -n "${TORCH_CUDA_ARCH_LIST:-}" ]; then
+        echo "Using caller-provided TORCH_CUDA_ARCH_LIST=${TORCH_CUDA_ARCH_LIST}"
+    elif [ "$cuda_major" -gt 12 ] || { [ "$cuda_major" -eq 12 ] && [ "$cuda_minor" -ge 8 ]; }; then
         # Include Blackwell support for CUDA 12.8+
         export TORCH_CUDA_ARCH_LIST="7.0;8.0;9.0;10.0"
     else
         export TORCH_CUDA_ARCH_LIST="7.0;8.0;9.0"
     fi

     uv pip install mplib==0.2.1 gymnasium==0.29.1 av open3d zarr openai
@@ -2697,7 +2699,9 @@ install_robotwin_env() {

     uv pip install git+${GITHUB_PREFIX}https://github.com/facebookresearch/pytorch3d.git@v0.7.9  --no-build-isolation
     uv pip install warp-lang==1.11.1
-    uv pip install git+${GITHUB_PREFIX}https://github.com/NVlabs/curobo.git  --no-build-isolation
+    SETUPTOOLS_SCM_PRETEND_VERSION_FOR_NVIDIA_CUROBO=0.7.8 \
+        uv pip install git+${GITHUB_PREFIX}https://github.com/NVlabs/curobo.git@v0.7.8 \
+        --no-build-isolation

     # patch sapien and mplib for robotwin
     SAPIEN_LOCATION=$(uv pip show sapien | grep 'Location' | awk '{print $2}')/sapien
