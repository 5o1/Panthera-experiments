# OpenVLA-OFT 多卡 action head 同步修复报告：复审记录

对应正文：`docs/25_openvla_action_head_ddp_sync_fix_2026-09-23.md`

## A. 证据清点

| 编号 | 报告准备使用的结论 | 项目内证据或外部来源 | 证据能支持到什么程度 | 缺失或限制 |
|---|---|---|---|---|
| A1 | 原训练调用绕过 DDP forward | 实际安装的 `vla-scripts/finetune.py`；上游 issue #160 | 能确认 L1/diffusion 的具体调用 | issue 本身不是修复依据，仍需本地实测 |
| A2 | L1 旧路径会跨卡分叉 | `docs/evidence/openvla_action_head_ddp_prefix_2026-09-22.json` | 三卡、一步 AdamW、给定 batch/LR 下的因果对照 | 不是长训练统计 |
| A3 | 修复后 L1 与 diffusion 同步 | `docs/evidence/openvla_action_head_ddp_postfix_2026-09-23.json` | 三卡上梯度、参数、共同输入输出均为零差异 | 没有覆盖其他 action head |
| A4 | 历史 28D 运行受影响 | 历史 `run-config.json`；checkpoint 保存代码 | 能确认该运行是三卡 L1 并只由主进程保存 action head | 未保存每个 rank 的历史权重，无法回溯分叉曲线 |
| A5 | 修复没有证明抖动已消失 | 本次仅一步 action-head audit | 能限定当前结论边界 | 需要修复后成对长训练和闭环评测 |
| A6 | DDP forward 是同步点 | PyTorch DDP 官方文档 | 支持机制解释 | 文档版本会更新，已记录访问日期 |
| A7 | bootstrap 状态曾漂移 | bootstrap 失败日志；stale patch 归档；补丁 SHA-256 | 能说明此次现场状态与处理 | 临时逆补丁不作为正式 overlay 功能 |

## B. 结构设计

| 章节 | 本节回答的问题 | 使用的证据 | 读者此前已学到的概念 | 是否存在无铺垫内容 |
|---|---|---|---|---|
| 1–2 | 修了什么、适用到哪里 | A1–A5、任务文档 | 无 | 否 |
| 3 | DDP 正常应怎样工作 | A6 | rank、batch、参数 | 否，先用行为再给公式 |
| 4 | 代码为何静默出错 | A1、保存代码 | DDP forward | 否 |
| 5 | 如何证明 issue 在本项目真实存在 | A2 | 梯度、参数、输出 | 否 |
| 6 | 补丁如何修复且如何进入 runtime | patch、bootstrap | action head、overlay | 否 |
| 7 | 修复后的数据是什么 | A3、聚焦测试 | 负对照 | 否 |
| 8 | 旧 checkpoint 还能说明什么 | A4、A5 | rank 0 保存 | 否 |
| 9–10 | 如何复现、下一步门禁是什么 | 脚本、测试、历史配置 | Lab/runtime | 否 |
| 11–12 | 外部与内部证据在哪里 | A1–A7 | 引用 | 否 |

## 第一轮：事实与引用

| 检查项 | 结论（通过／需修改／不适用） | 检查证据 | 发现的问题 | 修改或保留限制 | 复查结果 |
|---|---|---|---|---|---|
| 标出所有定量结果、完成状态、版本和因果陈述，逐项回到原始证据 | 需修改 | 两份 evidence JSON、Lab `run-config.json`、安装包 `direct_url.json`、补丁与 bootstrap 输出 | 初稿把最小随机 batch 写成与真实训练条件一致，范围过宽 | 改成“只保留判断梯度归并的关键条件”，明确未加载完整 VLA/RLDS | 通过；表中数字与 JSON 一致，版本与安装元数据一致 |
| 检查单臂/双臂、仿真/真机、训练/评测、开发集/测试集等边界是否被混淆 | 通过 | 报告第 2、8 节；AGENTS 当前边界 | 未发现混淆 | 明确写为单 Panthera、训练器内部、未启动真机 | 通过 |
| 查出“模型负责安全检查”“输入约束等同于硬约束”等职责误写 | 通过 | 报告第 2、9、10 节 | 未把 DDP audit 写成真机安全门 | 保留“未访问真机”和训练前门禁表述 | 通过 |
| 验证每个外部链接、作者、标题、年份和正文用途 | 通过 | PyTorch 官方 DDP 文档；OpenVLA-OFT issue #160；安装包 `direct_url.json` | RLinf 固定提交的网页抓取不可用，但本地安装元数据给出 URL 和完整 commit | 参考资料 [3] 仅作为安装来源定位，不用它支持 DDP 因果结论 | 通过 |
| 把推断写成推断，把缺失结果写成缺失 | 通过 | 报告第 1、8、10 节 | 无法由一步实验推出长期抖动变化 | 明确写出“尚未成对长训练”“不能证明唯一原因” | 通过 |

第一轮完成：定量结果、版本、调用路径和历史运行配置均能回到证据；唯一过宽表述已经收窄。尚未执行的长训练没有写成完成状态。

## 第二轮：初学者阅读连续性

| 检查项 | 结论（通过／需修改／不适用） | 检查位置 | 初学者会卡在哪里 | 直接改写结果 | 复查结果 |
|---|---|---|---|---|---|
| 读者遇到缩写前是否已经看到全称和用途 | 通过 | 第 1、2 节 | DDP 已在首次出现处展开；proprio 原来没有普通语言定义 | 第 2 节补充 proprio 的四个 7 维输入 | 通过 |
| 是否把配置名、代码变量或团队内部简写当作自然语言 | 需修改 | 第 2、6.3 节 | action head、rank、overlay 只有代码名，没有先说明职责 | 分别说明动作头输入输出、每 GPU 进程、受管修改层 | 通过 |
| 保留的正式术语是否必要且首次出现已解释 | 通过 | 第 2、3 节 | action head、DDP、rank 后文反复使用，必须保留 | 先描述行为，再保留英文检索名 | 通过 |
| “增益、死区、滤波、锁存、容差、p95、峰峰值”等统计或控制词是否具体 | 不适用 | 全文 | 正文未使用这些词 | 不适用 | 不适用 |
| 新模块是否说明输入、输出及其在链路中的位置 | 通过 | 第 2、6、7 节 | 初稿未明确 action head 的输入输出 | 补充“隐藏特征 → 25×7 连续动作” | 通过 |
| 每个代词是否有唯一指代 | 通过 | 全文通读 | 未发现无法定位的“它”“该方法” | 无需修改 | 通过 |
| 相关工作是否解释机制而不只列名称 | 通过 | 第 3、4 节 | issue 只用于定位，机制由 DDP 行为和本地对照解释 | 保持现有写法 | 通过 |
| 公式前是否定义符号，公式后是否说明用途 | 通过 | 第 3 节 | `θ`、`g_r`、`N` 在公式附近定义充分 | 公式后解释为何一步即分叉 | 通过 |
| 教程是否说明机器、目录和环境 | 通过 | 第 9 节 | 无 | 已写 Lab、普通用户、绝对目录、不访问真机 | 通过 |

只读到第 3 节时，读者已经能回答：每张 GPU 拿到不同局部 batch，action head 把隐藏特征变成动作，DDP 应把各卡梯度归并后再更新。第二轮通过。

## 第三轮：方法与复现

| 检查项 | 结论（通过／需修改／不适用） | 代码、配置或数据证据 | 缺少的复现信息 | 修改或限制 | 复查结果 |
|---|---|---|---|---|---|
| 数据来源、成功筛选、训练/验证/测试划分是否清楚 | 不适用 | audit 以确定随机种子生成张量，不训练任务数据集 | 无任务数据划分 | 报告明确它是一步同步诊断，不是策略性能实验 | 不适用 |
| 输入输出维度、单位、坐标系和归一化是否明确 | 需修改 | audit 脚本；两份 JSON | 初稿没有写诊断缩小后的隐藏宽度和随机种子 | 第 5.1 节补充 L1/diffusion 宽度、三个种子及 25×7 输出；本实验无物理单位/坐标系 | 通过 |
| 冻结模块、训练模块、损失和关键超参数是否明确 | 通过 | audit `_new_head`、`_run_case` | 无完整 VLA，所以不存在冻结模块 | 写明只实例化 action head；L1/MSE、AdamW、batch、LR 均可定位 | 通过 |
| 上游软件原有能力与本项目自定义代码是否分开 | 通过 | 第 4、6、7.1 节；overlay patch；上游 PR commit `bf0b44c` | 无 | 上游错误调用、Panthera patch、受管 runtime 和独立 PR 分支分开说明；PR 不含项目定制 | 通过 |
| 评测是否包含模型加载、环境执行、成功条件和失败分类 | 不适用 | 第 5、7 节 | 不是 RoboTwin 闭环评测 | 把“回归成功”限定为跨卡同步阈值，未冒充任务成功 | 不适用 |
| 命令、配置路径和提交号是否真实可用 | 通过 | 第 7.1、9、12 节；Lab 实际执行日志；上游 PR #162 | 无 | runtime 命令已实跑；安装提交、上游 PR base、修复 commit 和 PR URL 均已核对 | 通过 |
| 教程每一步是否有预期输出、验收条件和停止条件 | 通过 | 第 9 节 | 无 | 写明 `status: pass`、`1e-7` 正验证、负对照阈值及 GPU 占用时停止/换空闲卡 | 通过 |

第三轮完成：读者能够重新装配 runtime、运行三卡 audit，并从 JSON 独立判断通过或失败；报告没有把同步测试写成闭环策略评测。

## 第四轮：中文语言与 PDF 版式

| 检查项 | 结论（通过／需修改／不适用） | 检查位置或页码 | 发现的问题 | 修改结果 | 复查结果 |
|---|---|---|---|---|---|
| 搜索“不是”“而是”“并非”“值得注意的是”“这意味着”，逐句判断是否必要 | 通过 | 第 4、6、7、9 节 | 共 5 处“不是”、1 处“而是”；均用于区分底层模块/仓库类型、异常/静默分叉或换卡/抢占等实质边界 | 未保留空泛转折；“值得注意的是”“这意味着”“并非”为 0 | 通过 |
| 合并连续的一句一段，拆开概念过多的长句 | 通过 | 全文通读 | 表格前后的单句多为范围声明或结果结论，不形成连续口号式段落 | 长步骤使用编号和表格；核心结果段保持相邻因果 | 通过 |
| 删除重复结论、空泛评价和没有信息增量的过渡句 | 通过 | 第 1、7、8 节 | 第 1 节给结论，第 7 节给完整数据，第 8 节给历史影响，职责不同 | 未发现可删除的重复段 | 通过 |
| 保留明确主语，减少名词堆叠和英文直译语序 | 通过 | 全文 | DDP、action head、rank、overlay 必须保留以对应代码 | 首次出现均有中文行为解释 | 通过 |
| 图题、表题、正文引用及编号一致 | 通过 | 结果表、参考资料 [1]–[3] | 自动检查最初发现参考资料 [3] 未在正文引用 | 在软件版本表前加入 `[3]`；复查正文引用集合与定义集合均为 `{1,2,3}` | 通过 |
| 标题、表格和代码块 PDF 分页正确 | 不适用 | Markdown 交付 | 用户未要求 HTML/PDF，本次不渲染 PDF | 保留 Markdown，避免生成与需求无关的派生产物 | 不适用 |
| PDF 无空白页、链接可点击、中文字体没有缺字 | 不适用 | Markdown 交付 | 无 PDF | 本地 Markdown 链接已逐个检查存在 | 不适用 |

自动检查命令及结果：

```text
python3 -m py_compile packages/panthera_vla/audit_openvla_action_head_ddp.py  # 通过
bash -n bin/run_lab_openvla_action_head_ddp_audit.sh                        # 通过
python3 -m json.tool docs/evidence/openvla_action_head_ddp_*.json            # 两份均通过
自定义 Markdown 本地链接和 [1]–[3] 引用闭合检查                               # 通过
git diff --check                                                              # 通过
Lab pytest: pipelines + assembler + packages/panthera_vla                    # 96 passed
OpenVLA-OFT PR: loader isolation + two-rank CPU/Gloo L1/diffusion            # 3 passed, 2 subtests passed
OpenVLA-OFT PR: Ruff lint/format + git diff --check                          # 通过
```

人工抽查页及结论：

```text
本次只交付 Markdown，无页码。人工通读结论、根因、两张结果表、复现命令、历史影响和参考资料；
表格列数一致，代码围栏闭合，本地证据链接存在，外部链接指向官方文档、原始 issue 和安装来源仓库。
```

## 最终门禁

- [x] A、B 两部分已经填写，没有把缺失证据写成事实。
- [x] 四轮复审按顺序完成，每个检查项都有结论、证据和处理结果。
- [x] 所有“需修改”项已经复查；未解决项已写入报告限制。
- [x] Markdown 正文和 review 文件互相对应；本次未要求 PDF。
- [x] 最终答复分别报告四轮结果，没有只说“已 review”。

仍未解决的限制：尚未用修复后的训练器重复 24 小时三卡训练，因此无法量化 DDP 修复对长期抖动、松爪或闭环成功率的影响；报告只确认多卡同步错误及其代码修复。
