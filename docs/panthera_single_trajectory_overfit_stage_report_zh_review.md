# Panthera 单轨迹过拟合阶段报告复审记录

## A. 证据清点

| 编号 | 论文准备使用的结论 | 项目内证据或外部来源 | 证据能支持到什么程度 | 缺失或限制 |
|---|---|---|---|---|
| A1 | Gate 2 固定为 episode 2 的同场景、同相机、单轨迹过拟合，不测试泛化 | `docs/22_single_trajectory_overfit_gate_hardening_2026-09-20.md`；Lab subset 与 `run-config.json` | 支持 seed、姿态、1037 个唯一 50 Hz 样本和 25×7 动作合同 | 只有一个 episode，不能报告泛化成功率 |
| A2 | 早期评测存在动作预算、相机和颜色通道错配，相关 0/18 不能评价策略 | `docs/18_eval_harness_defects_2026-09-19.md`；`docs/20_eval_observation_mismatch_2026-09-19.md` | 支持缺陷机制、修复及“旧数字作废”结论 | 这些记录用于解释门禁演化，不与最终单轨迹结果合并统计 |
| A3 | 旧 7 维 24 小时 run 保存 17 个 checkpoint，13 个有成对 h20/h1 报告，26 个模式 rollout 均未成功 | Lab 旧 run 的 `run-config.json`、`checkpoints.json`、13 个 `pair-summary.json`；`docs/evidence/single_trajectory_overfit_stage_2026-09-24.json` | 支持运行配置和 0/26 的直接计数 | 训练器后来被证实绕过 DDP，不能作为正确的 7D 因果基线 |
| A4 | step 65000 滑动平均能到释放位但持续闭合，失败集中于阶段识别/释放 | `docs/22_single_trajectory_overfit_gate_hardening_2026-09-20.md` 第 8 节及 Lab trace | 支持该 checkpoint 的位置、夹爪范围和人工视频审阅 | 单 checkpoint 诊断，不能概括所有模型 |
| A5 | 28 维状态为 qpos、qvel、qacc、effort 四个七维块，派生集经确定性重放并通过误差门禁 | `docs/24_openvla_dynamics_proprioception_overfit_2026-09-21.md`；构建脚本和 smoke 产物 | 支持维度、物理含义、重放误差和两步优化器 smoke | 派生状态不是采集时原生记录，只适用于单轨迹诊断 |
| A6 | OpenVLA-OFT 多卡连续动作头绕过 DDP，三卡参数在一步后分叉；修复后 L1/diffusion 差异为 0 | `docs/25_openvla_action_head_ddp_sync_fix_2026-09-23.md`；前后 JSON；补丁与回归脚本 | 支持根因、数值对照、修复实现及 Lab preflight | 一步审计证明同步正确，不单独证明闭环抖动全部由此造成 |
| A7 | 修复后 28 维 run 使用 3×RTX 4090、每卡 batch 6、学习率 5e-4、24 小时、每 5000 步保存，共 17 个 checkpoint | Lab `run-config.json`、`checkpoints.json`、`TRAINING_COMPLETE`、exit code 0；A6 报告 | 支持配置、完成状态和 checkpoint 分母 | 当前 WSL 工作树未冻结为最终公开复现提交 |
| A8 | 最终 34 个模式 rollout 中 4 次准时成功，来自 step 30000、45000、75000；step 75000 两种模式均成功；无延迟成功 | 最终 `summary.json`；`docs/evidence/single_trajectory_overfit_stage_2026-09-24.json` | 支持精确 checkpoint、模式、执行步数和成功分类 | 同一 episode 的重复模型评测，不是 34 个独立场景 |
| A9 | step 80000 离线 L1 低于 75000 却闭环失败；80000 卡在松爪，85000 退化到抬起前 | 三个 checkpoint 的最终 JSON trace；本地紧凑证据 JSON | 支持末态、夹爪命令、插入深度、抬升和物体位移 | 解释是确定性轨迹上的机制诊断，不是统计因果估计 |
| A10 | 当前系统相对旧 7 维 run 有闭环能力进步，但不能把进步单独归因于 28 维状态 | A3、A6–A9 | 支持“系统组合改善”和“因果混杂”两部分 | 缺少使用修复后训练器的 7 维成对重训 |
| A11 | h1 等权时序融合行为更平滑但单 GPU 策略推理远慢于 50 Hz；h20 可摊销到实时预算内 | `docs/22_single_trajectory_overfit_gate_hardening_2026-09-20.md` 第 6 节 | 支持旧 run 上 128.59 ms/action 与 h20 6.45 ms/action 的策略侧下界 | 计时不含相机、ROS、仿真渲染或真实机器人；不是端到端延迟 |
| A12 | 最终评测产物齐全：17 个 pair summary、17 个双模式视频、两类各 17 个单模式视频、总视频和完成标志 | Lab 产物核验与 ffprobe；证据 JSON | 支持数量、非空及可解码性 | 视频和大 JSON 仅保存在 Lab，尚未打包公开 |
| A13 | OpenVLA-OFT 使用并行动作解码、动作块、连续动作表示和 L1 目标 | Kim、Finn、Liang 的 OpenVLA-OFT 论文和官方项目页 | 支持上游方法背景 | 上游 LIBERO/ALOHA 结果不能转写为 Panthera 性能 |
| A14 | PyTorch DDP 依靠包装器的 forward/backward 路径同步副本梯度 | PyTorch `DistributedDataParallel` 官方文档 | 支持 DDP 机制与本次绕过 forward 的风险 | 项目故障是否实际发生仍由 A6 的本机对照证明 |
| A15 | 行为克隆在闭环中会遇到专家数据未覆盖的状态；DAgger 是后续收集恢复状态数据的候选 | Ross、Gordon、Bagnell 2011；项目闭环 trace | 支持分布偏移背景和候选方向 | 本阶段没有实现 DAgger，不写成已采用方法 |

## B. 结构设计

| 章节 | 本节回答的问题 | 使用的证据 | 读者此前已学到的概念 | 是否存在无铺垫内容 |
|---|---|---|---|---|
| 摘要与结论边界 | 从最初门禁到当前取得了什么，哪些仍未证明 | A1–A12 | 无 | 否 |
| 1 研究问题与任务边界 | 为什么先做单轨迹过拟合，成功代表什么 | A1、A2 | 摘要中的单臂圆柱入槽 | 否 |
| 2 初学者所需概念 | 动作块、闭环、离线 L1、proprio 和 DDP 分别是什么 | A5、A13–A15 | 图像、关节状态、动作 | 否 |
| 3 实验对象与统一合同 | episode 2、25×7、两种执行模式和成功预算怎样定义 | A1、A3、A7、A8 | 第 2 节定义的术语 | 否 |
| 4 时间线：评测台先于模型 | 哪些早期数字失效，为什么必须先修观测和预算 | A2 | 合同和闭环 | 否 |
| 5 旧 7 维运行 | 24 小时训练做到了哪里，释放为何失败 | A3、A4、A11 | h20/h1 与夹爪阈值 | 否 |
| 6 28 维与 DDP 审计 | 为什么加动力学状态，随后发现什么训练器缺陷 | A5、A6、A14 | proprio、梯度和副本 | 否 |
| 7 修复后正式重训 | 数据、硬件、超参数、checkpoint 和评测怎样组织 | A7、A8、A12 | 前述实验合同 | 否 |
| 8 最终结果 | 哪些 checkpoint 成功，何时完成 | A8 | 成功分类 | 否 |
| 9 非单调失败机制 | 为什么 80000/85000 比 75000 差，loss 为何不能选模 | A9、A15 | 专家状态与闭环状态 | 否 |
| 10 相对进步与因果边界 | 28 维是进步还是退步，能否单独归因 | A3、A6、A8、A10 | 对照变量 | 否 |
| 11 复现和产物检查 | 在哪台机器、什么目录、用什么命令核验 | A7、A12 | 运行目录和门禁 | 否 |
| 12 局限与下一步 | 还缺哪些对照和泛化实验 | A1、A10、A15 | 全文 | 否 |
| 参考文献与证据索引 | 外部方法和内部结论在哪里核验 | A1–A15 | 全文 | 否 |

## 第一轮：事实与引用

| 检查项 | 结论（通过／需修改／不适用） | 检查证据 | 发现的问题 | 修改或保留限制 | 复查结果 |
|---|---|---|---|---|---|
| 标出所有定量结果、完成状态、版本和因果陈述，逐项回到原始证据 | 需修改 | E1–E8；Lab 两个 run 的 `run-config.json`、`checkpoints.json`、最终 `summary.json` 与关键 trace | 旧七维 run 使用 DDP 故障路径可由时间和相同训练入口推断，但旧 run 没有运行前 audit；初稿语气过于确定。当前 WSL 有未提交修改，不能给出一个代表全部实验代码的最终提交号 | 已把旧七维表述改为“修复前同一入口、未保存 audit，保守视为可能受影响”；第 11 节列出三个上游提交，并明确 WSL 仅有基准 HEAD、工作树未冻结 | 通过：重新逐项核对表 1–3、摘要成功分母和版本段，未把推断写成已证实因果 |
| 检查单臂/双臂、仿真/真机、训练/评测、开发集/测试集等边界是否被混淆 | 通过 | 摘要、第 1、3、7、12、13 节 | 无；全文限定为单 Panthera、单夹爪、仿真 episode 2，同一训练轨迹，不报告泛化或真机结论 | 保留边界 | 通过 |
| 查出“模型负责安全检查”“输入约束等同于硬约束”等职责误写 | 通过 | 第 2.1、3、11、12 节 | 无；模型只输出动作，成功判定、GPU 占用拒绝和真机门禁由确定性程序负责 | 保留职责划分 | 通过 |
| 验证每个外部链接、作者、标题、年份和正文用途 | 需修改 | OpenVLA-OFT RSS 页面；ACT arXiv；DAgger PMLR；PyTorch DDP 官方文档；OpenVLA PMLR | [1]–[4] 均在正文有直接用途；[5] OpenVLA 已列入参考文献但正文未引用 | 2.1 节已补 OpenVLA 基础模型背景并引用 [5] | 通过：自动审计显示正文 [1]–[5] 与参考文献 [1]–[5] 闭合，无缺失和未引用条目；五个链接均为论文或官方文档原页 |
| 把推断写成推断，把缺失结果写成缺失 | 需修改 | 第 10、12 节；A3、A10 | 28 维输入的独立贡献已经降级，但旧七维 DDP 影响仍写成无条件事实；缺少修复后七维重训已写入限制 | 已收窄 DDP 历史归因，保留“系统组合改善、28D 独立贡献未知”，并把修复后七维重训列为下一步 | 通过：摘要、第 6.2、10、12、13 节均使用相同因果边界 |

第一轮发现三处问题并完成修改。HTML 引用审计通过，紧凑证据 JSON 与正文的 17 个 checkpoint、4 次模式成功、3 个成功 checkpoint 和关键 L1 数字交叉核对通过。

## 第二轮：初学者阅读连续性

| 检查项 | 结论（通过／需修改／不适用） | 检查位置 | 初学者会卡在哪里 | 直接改写结果 | 复查结果 |
|---|---|---|---|---|---|
| 读者遇到缩写前是否已经看到全称和用途？ | 需修改 | 摘要、第 2、3、4、7 节 | VLA、DDP 已展开；qpos/qvel/qacc/effort、LoRA、mrad 和 rollout 未在首次出现处解释 | 已在首次出现处先写关节位置/速度/加速度/载荷、参数高效微调、毫弧度和连续闭环执行，再保留代码名；图 1 删除先于定义出现的 LoRA 标签 | 通过：从摘要顺读到第 7 节，所有保留缩写均能回指首次定义 |
| 正文是否把配置名、代码变量或团队内部简写当作自然语言？ | 需修改 | 第 1、3、6、8 节 | `fixed-camera`、Gate 2、h20 和 step 属于内部表达；h20 已解释，但其他词仍可能承担主要语义 | 已改为“固定相机数据”“单轨迹门禁”“模型存档”；表 3 明确 step 是累计优化步数；h20 在 2.2 节保留为后文复用的精确模式名 | 通过：内部名称均放在普通中文行为说明之后 |
| 每个保留的正式术语是否确实会在后文复用，或能帮助读者查阅资料？首次出现时是否先说明它解决的具体问题？ | 需修改 | 第 2.3、2.4、6.2 节 | “闭环 rollout”“DDP forward”有技术用途，但 rollout 没有中文行为定义 | 已先说明“把模型动作送入环境、读取新图像并继续预测”，再命名 rollout；DDP forward 由第 2.4 节先解释同步职责 | 通过：两个术语均在方法、结果和复现部分重复使用且含义稳定 |
| “增益、死区、滤波、锁存、容差、p95、峰峰值”等词若出现，读者能否在同一句读到具体计算或可观察结果？ | 通过 | 全文检索 | 没有这些未解释统计缩写；P99 已改写为“99% 的样本不超过” | 保留当前写法 | 通过 |
| 新模块出现时是否说明输入、输出及其在整条链路中的位置？ | 通过 | 第 2 节、图 1、第 6.2 节 | VLA、连续动作头、离线验证、闭环评测和 DDP 均有输入/处理/输出 | 保留 | 通过 |
| 每个“它”“该方法”“上述结果”能否唯一定位指代对象？ | 需修改 | 第 6.1、10 节 | “其余三块”和“当前组合”可回指，但需要读者跨句解析 | 已直接列出速度、加速度和载荷；“当前组合”已改为“28 维输入与修复后 DDP 组成的系统” | 通过：全文顺读未发现需要跨节猜测的代词 |
| 相关工作是否解释方法机制，而不只列名称？ | 通过 | 第 2.1–2.4 节 | OpenVLA-OFT、ACT/时序融合、DAgger 和 DDP 都说明了处理机制及本项目采用范围 | 保留 | 通过 |
| 公式前是否定义符号，公式后是否解释结果如何用于项目？ | 不适用 | 全文 | 报告没有依赖数学公式，只给出直接测量值和阈值 | 无需为了形式增加公式 | 不适用 |
| 教程是否说明在哪台机器、哪个目录和哪个环境运行？ | 通过 | 第 11 节 | 已写 Lab、普通用户、项目目录和激活入口 | 保留；第三轮再核验命令真实性 | 通过 |

“只读到本段”复查：读完第 2 节可以说明模型拿到图像、文字和当前状态，输出 25×7 动作；读完第 3 节可以说明七维与 28 维状态的差别和两个预算；读完第 7 节可以说明训练如何保存候选并由两种模式闭环评测。第二轮完成。

## 第三轮：方法与复现

| 检查项 | 结论（通过／需修改／不适用） | 代码、配置或数据证据 | 缺少的复现信息 | 修改或限制 | 复查结果 |
|---|---|---|---|---|---|
| 数据来源、成功筛选、训练/验证/测试划分是否清楚？ | 需修改 | 第 1、3、7 节；旧/新 `run-config.json`；`docs/22`、`docs/24` | 已说明 episode 2，但未给出源数据与 28D 派生数据绝对路径；同一轨迹同时用于训练和离线验证的边界可再明确 | 第 3 节已补两个绝对路径、run-local subset，并明确无独立训练/验证/测试划分，只作健全性门禁 | 通过：Lab 只读核验两个源目录存在 |
| 输入输出维度、单位、坐标系和归一化是否明确？ | 需修改 | 表 1；`place_cylinder_in_groove._actual_robot_state()`；28D RLDS 统计 | 维度清楚，但六关节/夹爪及速度、加速度、载荷单位未完整列出 | 已补六旋转关节 rad、rad/s、rad/s²、N·m；夹爪位置、速度、加速度为按行程归一化量，载荷为 N；28 维逐维统计归一化 | 通过：与 `_actual_robot_state()` 的 span 换算和 incoming force 轴一致 |
| 冻结模块、训练模块、损失和关键超参数是否明确？ | 需修改 | 第 2.1、7 节；Gate 入口参数；checkpoint 中 LoRA/action head/proprio projector | LoRA、batch、学习率已写，但没有明确列出训练的三个组件和基础权重职责；L1 是归一化动作空间还是物理单位需说明 | 已写 LoRA adapter、连续 action head、proprio projector 参与训练，基础模型其余权重不做完整微调；loss 在逐维归一化 25×7 动作上计算 | 通过：与入口参数和三个 checkpoint 组件一致 |
| 上游软件原有能力与本项目自定义代码是否分开？ | 需修改 | 第 2、6、11 节；OpenVLA-OFT 论文；Panthera overlay 与评测脚本 | 方法背景和 DDP 补丁已区分，但没有集中列出 Panthera 新增的数据访问、runtime overlay、rollout 和产物队列 | 已增加 6.3 节职责表，分开 OpenVLA-OFT、RoboTwin/SAPIEN 与 Panthera 数据/评测层 | 通过：每行均给出上游能力和项目新增文件或行为 |
| 评测是否包含模型加载、环境执行、成功条件和失败分类？ | 需修改 | `rollout.py`；`place_vertical_cylinder_in_groove._metrics_pass`；最终 summary | 模型/环境/预算和三类完成状态已写；成功判定只写概念，没有数值阈值与稳定检查 | 已增加表 4：XY、Z、轴角、插入深度、线/角速度、开爪、接触冲量和连续三次稳定检查 | 通过：阈值逐项与 `_metrics_pass` 源码一致 |
| 命令、配置路径和提交号是否真实可用？ | 通过 | `tools/activate_lab_vla.sh`、`bin/run_lab_openvla_action_head_ddp_audit.sh`、Lab 最终目录、`externals/pinned.json` | 无；三个上游提交和 WSL 未冻结状态已写 | 保留；写作期间不执行高成本训练 | 通过 |
| 教程每一步是否有预期输出、验收条件和停止条件？ | 需修改 | 第 11 节；DDP audit 入口；最终产物 | 步骤 1–3 有停止条件；步骤 4 只有观察内容，没有明确文件缺失时停止 | 已补视频、`pair-summary.json` 缺失或 ffprobe 失败时停止选模；明确 DDP audit 需空闲 GPU，写作不启动训练 | 通过：三个 shell 入口通过 `bash -n`，Lab 只读核验入口、数据路径、17 个汇总和总视频均存在 |

第三轮完成。只读 Lab 核验返回 `checkpoints 17`、`success_modes 4`、`delayed_modes 0`、`method_repro_readonly=PASS`；没有因报告写作启动训练、评测或真机。

## 第四轮：中文语言与 PDF 版式

| 检查项 | 结论（通过／需修改／不适用） | 检查位置或页码 | 发现的问题 | 修改结果 | 复查结果 |
|---|---|---|---|---|---|
| 搜索“不是”“而是”“并非”“值得注意的是”“这意味着”，逐句判断是否必要 | 需修改 | HTML 第 1、4、5、7、10、12、13 节 | 共出现“不是”5 次、“而是”2 次、“并非”1 次、“而非”1 次；多数可以直接陈述边界或结果，结论段的“不是……而是……”带模板感 | 改写为直接行为和证据，没有保留模板式转折 | 通过：最终自动审计中六个目标短语计数均为 0 |
| 合并连续的一句一段，拆开塞入过多概念的长句 | 需修改 | 摘要、第 6.2、11 节版本段 | 摘要结果句和第 11 节版本句较长；DDP 根因段同时说明错误、保存和历史影响 | 拆分版本、错误范围与历史证据边界；段落仍按同一论点组织 | 通过：人工顺读第 1、6、11 页，没有连续碎句或需要回读的指代 |
| 删除重复结论、空泛评价和没有信息增量的过渡句 | 需修改 | 摘要、第 8、10、13 节 | “系统取得进步”在第 10 节和结论重复；需要让第 10 节负责因果比较、结论只负责门禁状态 | 第 10 节保留成对因果边界；结论只收束已通过门禁、选模和未完成项 | 通过：摘要、比较和结论承担不同信息功能 |
| 保留明确主语，减少名词堆叠和英文直译语序 | 需修改 | 第 7 节、图 2、表 5 | “DDP preflight”“run”“step”等少量工程词仍可改成中文，图表中可保留 step 作为路径标签但正文不依赖它 | 正文改为启动前检查、运行、优化步数、模型存档和闭环执行；命令变量与代码路径保留原名 | 通过：图 2、表 5 和第 7–9 节均能脱离内部命名阅读 |
| 图题、表题、正文引用及编号一致 | 通过 | 图 1–3、表 1–5 | 插入职责表和成功条件表后已重新编号，无跳号 | 保留 | 通过 |
| 标题不孤悬在页尾；表格行、代码块和图题不被错误拆页 | 需修改 | PDF 第 1、2、4、6–8、10–12 页 | 首次渲染的封面标题末字单独换行；参考文献强制分页后第 11 页过空；取消分页后证据表只剩两行落在第 12 页 | 封面改为三行平衡标题；参考文献与结论同页，证据索引单独分页；表行和代码块保持不可拆分 | 通过：最终 12 页抽查未见孤行标题、表行截断、图题分离或内容裁切 |
| PDF 无空白页、链接可点击、标题元数据正确，中文字体没有缺字 | 需修改 | PDF 全文；重点抽查第 1、2、4、6–8、10–12 页 | 初稿尚无 PDF | 使用 Chrome 无头渲染；运行自动审计；将关键页渲染为 PNG 人工查看 | 通过：12 页、空白页 0、链接注释 8、标题元数据正确；抽查页中文显示完整 |
| 阶段报告没有因版式完整而被表述成最终研究结论 | 通过 | 摘要、第 1、10、12、13 节 | 明确单 episode、无泛化、无真机、28D 因果未分离 | 保留 | 通过 |

自动检查命令及结果：

```bash
/tmp/panthera-overfit-paper-audit/bin/python \
  /home/assaneko/.codex/skills/write-project-paper/scripts/audit_paper.py \
  docs/panthera_single_trajectory_overfit_stage_report_zh.html \
  --pdf docs/panthera_single_trajectory_overfit_stage_report_zh.pdf --json
```

结果：HTML 标题结构有效；正文使用引用 `[1]`–`[5]`，参考文献也恰为 `[1]`–`[5]`；
缺失引用与未引用条目均为 0；外链 5 个；目标模板短语均为 0；空表格单元 0。PDF 为
12 页，空白页 0，链接注释 8，标题元数据正确。`git diff --check` 通过。

人工抽查页及结论：第 1 页检查封面、摘要和标题换行；第 2、4、6 页检查概念、时间线、
表格和工程术语；第 7 页检查完整 17 存档结果表；第 8 页检查损失图和三个关键存档诊断；
第 10 页检查命令块与局限；第 11 页检查结论和五条参考文献；第 12 页检查八项证据索引。
未发现裁切、重叠、空白页、中文缺字或错误拆页。

## 最终门禁

- [x] A、B 两部分已经填写，没有把缺失证据写成事实。
- [x] 四轮复审按顺序完成，每个检查项都有结论、证据和处理结果。
- [x] 所有“需修改”项已经复查；未解决项已写入论文限制。
- [x] 源文件、PDF 和 review 文件互相对应。
- [x] 最终答复分别报告四轮结果，没有只说“已 review”。

仍未解决的限制：

1. 训练、离线诊断和闭环评测都使用 episode 2，没有场景泛化结论。
2. 旧七维运行可能受 DDP 绕过缺陷影响，缺少修复后七维成对重训，不能把改善单独归因于
   28 维状态。
3. 28 维状态由确定性重放派生，尚未由正式数据采集器原生记录。
4. 模型没有时间历史和失败恢复示范，释放阶段仍容易在轻微闭环偏移下混淆。
5. 逐步等权的策略推理速度尚不满足 50 Hz；现有计时也不含相机、ROS 和真机执行延迟。
6. 34 次模式评测共享同一场景，不能换算为总体成功率或置信区间。
7. Lab 大型产物尚未形成含哈希、环境锁文件和数据许可证的公开复现包；WSL 工作树也尚未
   冻结为最终发布提交。

---

## 逐版历史叙事实质修订复审（2026-09-24）

用户要求把报告改为“每一版发现问题、解决问题、再进入下一版”的历史叙事，并要求每版
都有 loss 图。本节取代上文旧稿的章节编号与版式结论；证据边界和未解决限制继续有效。

### 第一轮：事实与引用

| 检查项 | 初查 | 修改 | 复查 |
|---|---|---|---|
| 四次真实训练是否与运行记录一一对应 | 需修改：旧稿合并了 V1/V2，省略 V3 的独立结果 | 拆为 V1 7D 短训练、V2 7D 24 h、V3 DDP 修复前 28D、V4 DDP 修复后 28D | 通过 |
| 每版 loss 是否来自实际证据 | 需修改：旧稿只有 V4 离线 L1 | V1 只画唯一留存点；V2–V4 读取各自 W&amp;B 历史并叠加训练后离线 L1 | 通过；没有插值补造 V1 曲线 |
| V3 的“看起来成功”是否误写成严格成功 | 需修改：旧稿未独立呈现 | 写明 15k 因轴角 25.02° 失败，40k 因末端速度未稳定失败 | 通过 |
| 28D 与 DDP 修复的因果是否混淆 | 通过但需在每版重复边界 | V3 明确受错误 DDP 路径影响；V2 仅记为可能受影响；V4 同时改变两个因素 | 通过 |

### 第二轮：初学者阅读连续性

| 检查项 | 初查 | 修改 | 复查 |
|---|---|---|---|
| 读者能否理解为什么发生下一次训练 | 需修改：原稿从旧 7D 直接跳到 DDP 修复版 | 每版末尾加入“现象—诊断—下一版决策”，第 4 节增加问题—修复表 | 通过 |
| 训练 loss 与离线 L1 是否会被误认为同一指标 | 需修改 | 每张 V2–V4 图题和正文均说明蓝线为训练中 2500 步区间中位数，橙线为训练后 1037 状态重算 | 通过 |
| 早期证据缺失是否清楚 | 需修改 | V1 图只显示 27k/0.0166 单点，并直接标记原始历史未保留 | 通过 |
| 版本、存档、执行模式是否有定义 | 通过 | 保留第 2、3 节统一合同，各版本只写变化项 | 通过 |

### 第三轮：方法与复现

| 检查项 | 初查 | 修改 | 复查 |
|---|---|---|---|
| loss 来源能否追踪 | 需修改 | 记录 W&amp;B run：V2 `adeiyrzm`、V3 `nwcg39nh`、V4 `1s35hyv6`；证据 JSON 增加点数与步数范围 | 通过 |
| 各版训练条件与闭环分母是否明确 | 需修改 | V1 写 31500 步；V2 写 17 存档/13 对完整报告/26 rollout；V3 写 34 次严格失败；V4 写 17 存档/34 rollout | 通过 |
| 成功阈值和动作预算能否解释 V3→V4 | 通过 | 将 V3 的姿态/速度失败与 V4 的 1037/2074 双预算、连续三次稳定条件连写 | 通过 |
| 产物目录职责是否改变 | 不适用 | 报告和 review 留在 WSL；训练日志、视频、checkpoint 留在 Lab | 通过 |

### 第四轮：中文语言与 PDF 版式

| 检查项 | 初查 | 修改 | 复查 |
|---|---|---|---|
| 章节是否按 V1→V4 连续排列 | 需修改 | 重排为第 5–9 节，DDP 根因单列为 V3 与 V4 的过渡 | 通过 |
| 图表编号是否连续 | 需修改 | 图 1–6、表 1–6 重新编号 | 通过 |
| 模板化转折短语 | 需修改：新增摘要出现“并非” | 改为直接陈述“不是直接原因” | 通过：目标短语计数为 0 |
| PDF 分页、裁切、空白页和字体 | 待重新渲染 | 在最终 HTML 固定后重建 PDF，并以自动审计和页面图检查 | 结果见下方最终复查记录 |

本次修订的最终 PDF 页数、链接和人工抽查页码，以修订完成后运行的审计结果为准；不得沿用
上文旧稿的 12 页结论。

最终复查结果：PDF 共 14 页、空白页 0、链接注释 8，标题元数据正确。人工检查第 1 页
摘要、第 4 页版本表、第 5–7 页 V1–V3 loss 图、第 8–10 页 V4 配置/表格/loss 图，以及
第 12–14 页复现、结论和证据索引；未发现图被截断、标题孤悬、表格行断裂或中文缺字。
表 6 跨页时使用重复表头。自动审计确认引用 `[1]`–`[5]` 闭合、空表格单元为 0、目标
模板短语计数均为 0，`git diff --check` 通过。
